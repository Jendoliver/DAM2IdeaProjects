package com.apporelbotna;

public class Main
{
    public static void main(String[] args)
    {
        Encoder encoder = new Encoder();
        String toCode = "pasanen";
        encoder.encode(toCode);
        // Meter a fichero

    }
}
